spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/flowos
    username: flowos
    password: flowos
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

flowos:
    jwt:
      secret: "flowos-dev-jwt-secret-2026-change-this-before-production-9f7a3c2e8b1d"
      expiration-ms: 86400000   # 24 hours