# docker-compose.yml in your project root
services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: flowos
      POSTGRES_USER: flowos
      POSTGRES_PASSWORD: flowos
    ports:
      - "5432:5432"